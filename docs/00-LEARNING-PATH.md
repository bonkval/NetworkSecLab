# Learning Path

This folder is a course about the exact application in this repository. It assumes you know only basic computer use. Read the lessons in number order and keep the project open beside them.

## What you built

You built an integrated network-security lab with several cooperating boundaries:

1. A Flask application provides authentication, a responsive dashboard, interactive labs, detection rules, incidents, evidence verification, and local reports.
2. An SQLite event store persists normalized authentication, SNMP, configuration, and Suricata activity.
3. A UDP SNMP service receives and decodes real localhost datagrams generated by the built-in simulator or sent by lab devices.
4. A detection engine correlates credential-attack patterns and maps defensible matches to MITRE ATT&CK.
5. Standalone viewer and detector tools remain available for studying the original append-only authentication pipeline.

The important architectural idea is bounded responsibility. Transport, parsing, persistence, correlation, presentation, and notification are separate enough to test independently, while the web process coordinates them for the one-command demonstration.

## What every project file does

| Path | Purpose |
|---|---|
| `start.py` | One-command launcher that prepares the environment and starts the lab. |
| `server.py` | Production-style Waitress entry point. |
| `app/__init__.py` | Web routes, authentication, simulations, ingestion, incidents, reports, and CSRF protection. |
| `app/store.py` | SQLite event, job, rule, incident, activity, and evidence persistence. |
| `app/services.py` | Managed background SNMP receiver service. |
| `app/templates/` | Login, dashboard, detection, incident, and report views. |
| `app/static/css/style.css` | Responsive dark/light themes, dashboard components, labs, and reports. |
| `app/static/js/theme.js` | Persistent theme selection and animated theme transition. |
| `security/engine.py` | Credential correlation, ATT&CK mapping, and incident creation. |
| `network/snmp_traps.py` | SNMP BER decoding, classification, UDP receiver, and redacted audit output. |
| `network/simulator.py` | Hardware-free SNMP v2c packet generator and UDP sender. |
| `network/configurator.py` | Inventory validation, safe previews, backups, and explicit SSH application. |
| `app/database/*.db` | SQLite databases generated at runtime and excluded from Git. |
| `monitor/common.py` | Shared log-following generator. |
| `monitor/viewer.py` | Raw live event terminal. |
| `monitor/detector.py` | Parser, sliding-window state, alarm, popup, and CLI. |
| `monitor/__init__.py` | Marks `monitor` as an importable Python package. It is intentionally empty. |
| `assets/alarm.wav` | PCM audio played when the threshold is reached. |
| `logs/login_attempts.log` | Shared append-only event channel for the PoC. |
| `samples/suricata_eve.jsonl` | Safe sample data for the Suricata importer. |
| `tests/` | Unit and integration coverage, including real localhost UDP transport. |
| `requirements.txt` | Declares runtime dependencies. |
| `.gitignore` | Keeps generated/runtime files out of Git commits. |
| `README.md` | Quick setup and operating instructions. |

```text
Browser dashboard ──> Flask routes ──> SQLite event/incident store
       │                    │                       ▲
       │                    ├── detection engine ───┤
       │                    ├── SNMP UDP receiver ──┤
       │                    └── config safeguards ──┘
       └── labs/simulators send real local requests and UDP packets
```

## Recommended order

1. [How the local website runs](01-LOCAL-WEB-RUNTIME.md)
2. [Python and Flask, function by function](02-FLASK-AND-PYTHON.md)
3. [HTML, Jinja, and CSS](03-FRONTEND.md)
4. [SQLite, authentication, sessions, and CSRF](04-DATABASE-AND-SECURITY.md)
5. [The file-based event pipeline](05-LOGGING-PIPELINE.md)
6. [The detector and sliding time window](06-DETECTOR-ENGINE.md)
7. [The viewer, command-line options, audio, and threads](07-TERMINAL-TOOLS.md)
8. [Complete request flows](08-END-TO-END-FLOWS.md)
9. [Debugging, exercises, and production limitations](09-PRACTICE-AND-NEXT-STEPS.md)
10. [Syntax glossary](10-SYNTAX-GLOSSARY.md)
11. [Core function reference](11-FUNCTION-REFERENCE.md)

## How to study the code

Do not try to memorize it. For each lesson:

1. Read a small section.
2. Find the referenced code in the project.
3. Predict what will happen before running it.
4. Change one harmless value, such as a color or message.
5. run the program and observe the result.
6. Undo the experiment or commit it to a separate Git branch.

The goal is to build a mental model: input enters, code transforms it, state changes, and output appears.

## Commands used throughout

Run these from the project root:

```powershell
.venv\Scripts\Activate.ps1
python server.py
python -m monitor.viewer
python -m monitor.detector
```

Activation changes which `python` and `pip` executables PowerShell finds. It does not start the website. For normal use, `python start.py` prepares dependencies and starts the complete lab; `python server.py` starts it directly when the environment is already ready.

## A note about the demo password

The demo credentials are intentionally easy to remember:

```text
Email: ced@gmail.com
Password: 12345
```

The database stores a password hash, not the text `12345`. Nevertheless, `12345` is a weak password and must never be used in a real application.
